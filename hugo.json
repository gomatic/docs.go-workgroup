{
  "baseURL": "https://gomatic.github.io/docs.go-workgroup/",
  "title": "go-workgroup",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-workgroup — type-safe concurrent fan-out/fan-in worker groups for Go."
  }
}
